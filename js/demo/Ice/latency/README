A simple latency test that measures the basic call dispatch delay of
Ice.

To run the demo, first follow the instructions from the C++ Ice latency
demo README to start the server.

In a separate window, start the client:

$ node Client.js
